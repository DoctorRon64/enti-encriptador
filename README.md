# Encryptador
---
This project implements a symmetric encryption system using a Caesar cipher.
Messages are stored encrypted in a local file and can be recovered on startup.
A checksum is used to detect file tampering.
Developed in C++ using Visual Studio.
